template: teslaapi
products:
  - brand: Tesla
    description:
      generic: virtual wallbox using values generated by `Tesla` vehicle
requirements:
  description:
    en: This is a virtual wallbox that pulls charge status information via the Tesla API. The vehicle must be associated with the TeslaAPI loadpoint.
params:
render: |
  type: teslaapi
